let score = 50;

// Booleans
const threePointer = true; // 3 points
const basket = true; // 2 points
const freeThrow = false; // 1 point

// Goal:
// Figure out how many points we have at the end

// Code goes here
// If I made the 3-pointer, then add 3 to my score
if (threePointer) { // Run this code IF threePointer is true
    score += 3;
    //score = score + 3; ALSO WORKS
}

console.log(score);
