const x = 3;
const y = 2;

// Goal: print out the larger of the two
// values

if (x > y) {
    console.log(x);
} else if (y > x) { // y > x
    console.log(y);
} else { // y === x
    console.log("They are the same");
}
